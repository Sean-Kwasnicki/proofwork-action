This folder is a fixture, not a product.

It exists so GitHub Actions can run `Sean-Kwasnicki/proofwork-action@v1`
against a stranger-shaped app: one empty test, no real work.

The job is supposed to FAIL. A green tick here would mean the gate is broken.
